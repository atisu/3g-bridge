#!/bin/bash
#
# 3g-bridge     This shell script takes care of starting and stopping
#               3g-bridge. This is a RedHat style init script.
#
# chkconfig: 2345 90 10
# description: 3g-bridge connects various grid infrastructures
# processname: bridge
# config: /etc/3g-bridge.conf
# pidfile: /var/run/3g-bridge.pid

# Source function library.
. /etc/rc.d/init.d/functions

[ -f /usr/sbin/3g-bridge ] || exit 0
[ -f /etc/3g-bridge.conf ] || exit 0

RETVAL=0
prog=3g-bridge
user=3g-bridge
pidfile=/var/run/3g-bridge.pid

start() {
	# Start daemons.

	echo -n $"Starting $prog: "
	if ! test -f "$pidfile" ; then
	    touch "$pidfile"
	    chown $user "$pidfile"
	    if [ -x /usr/sbin/selinuxenabled ] && /usr/sbin/selinuxenabled; then
		/sbin/restorecon "$pidfile"
	    fi
	fi
	daemon --user $user /usr/sbin/3g-bridge -c /etc/3g-bridge.conf
	RETVAL=$?
        echo
        [ $RETVAL -eq 0 ] && touch /var/lock/subsys/3g-bridge
	return $RETVAL
}

reload() {
	# Stop daemons.
    	echo -n $"reloading $prog: "
	if pidfileofproc /usr/sbin/3g-bridge; then
		killproc "$prog" -HUP
	else
		failure $"Reloading $prog"
	fi
		RETVAL=$?
	echo
	return $RETVAL
}

stop() {
	# Stop daemons.
	echo -n $"Shutting down $prog: "
	killproc "$prog"
	RETVAL=$?
	echo
	[ $RETVAL -eq 0 ] && rm -f /var/lock/subsys/3g-bridge
	return $RETVAL
}

# See how we were called.
case "$1" in
  start)
	start
	;;
  stop)
	stop
	;;
  reload)
	reload
	RETVAL=$?
	;;
  restart)
	stop
	start
	RETVAL=$?
	;;
  condrestart)
	if [ -f /var/lock/subsys/3g-bridge ]; then
	    stop
	    start
	    RETVAL=$?
	fi
	;;
  status)
	status "$prog"
	RETVAL=$?
	;;
  *)
	echo $"Usage: $0 {start|stop|restart|condrestart|status}"
	exit 1
esac

exit $RETVAL
