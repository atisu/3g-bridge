#! /usr/bin/env python

from distutils.core import setup

setup(name = "G3Bridge",
      version = "@PACKAGE_VERSION@",
      description = "Python components of the 3G Bridge",
      packages = [ "G3Bridge" ])
